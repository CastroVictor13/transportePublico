server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transporte_publico
    username: root
    password: Anima123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
